services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: duetto
      POSTGRES_PASSWORD: duetto
      POSTGRES_DB: duetto
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
